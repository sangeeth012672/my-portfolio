name: flutter_app
description: "Flutter Developer Portfolio — A stunning, production-grade portfolio app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0-188.0.dev

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  animated_text_kit: ^4.2.3
  flutter_staggered_animations: ^1.1.1
  url_launcher: ^6.3.1
  font_awesome_flutter: ^10.8.0
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
